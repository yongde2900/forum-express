{{> msg}}
<div class="card d-flex flex-row">
    <img src="{{user.image}}" class="card-img-top rounded-circle" alt="..." style="width: 320px;">
    <div class="card-body">
        <h3>{{user.name}}</h3>
        <p>{{user.email}}</p>
        <a href="/users/{{user.id}}/edit"><button class="btn btn-primary">Edit</button></a>
    </div>
</div>
<br>
<div class="card">
    <nav class="navbar navbar-dark bg-light">
        <h3>{{commentedRestaurants.length}}家已評論的餐聽</h3>
    </nav>
    <div class="p-3  d-flex flex-row flex-wrap">
        {{#each commentedRestaurants}}
        <a href="/restaurants/{{this.RestaurantId}}" class="mr-3 my-1"><img src="{{this.RestaurantImage}}" alt=""
                style="width: 150px;"></a>
        {{/each}}
    </div>
</div>